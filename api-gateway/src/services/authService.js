async function getUserFromAuthMS(authHeader) {
  // TODO: send request to auth ms
  return {
    userId: "1234",
    userEmail: "someone@example.com",
  };
}

module.exports = {
  getUserFromAuthMS,
};
